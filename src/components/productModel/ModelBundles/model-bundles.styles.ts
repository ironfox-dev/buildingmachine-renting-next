import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    maxWidth: '30%',
    padding: '1rem',
    marginTop: '1rem',
  },

  key: {
    fontSize: '1.2rem',
    fontWeight: 'bold',
  },
});
