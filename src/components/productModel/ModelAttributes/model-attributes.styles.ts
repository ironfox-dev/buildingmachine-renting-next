import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    width: '50%',
    padding: '1rem',
  },

  key: {
    fontSize: '1.2rem',
    fontWeight: 'bold',
  },
});
