import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  header: {
    fontSize: '2.2rem',
  },
  modelImage: {
    maxHeight: 70,
    minWidth: 60,
    marginRight: 20,
  },
});
