import { createStyles } from '@material-ui/core/styles';

const bglDataStyle = createStyles({
  shortInputComponent: {
    width: '32%',
    marginBottom: '2rem',
  },
  bglDetailContainer: {
    padding: '1rem 0',
  },
});

export default bglDataStyle;
